import * as series from './mixins/series';
import * as xy from './mixins/xy';
import xyValues from './mixins/xy-values';

const mixins = {
  series,
  xy,
  xyValues
};

export {
  mixins as default,
  series,
  xy,
  xyValues
};
